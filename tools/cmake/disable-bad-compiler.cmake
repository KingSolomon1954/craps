set(CMAKE_C_COMPILER_WORKS 1)
set(CMAKE_CXX_COMPILER_WORKS 1)
#    Ignore CMake's compiler test. It reports failure
#    but it can be ignored. We're using an alternate
#    compiler which must be confusing CMake.
#    These must be defined prior to project()
